---
phase: 04-trust-bar-results-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/checks/robot-weight.check.ts
  - src/server/checks/robot-weight.check.test.ts
  - src/server/checks/robot-bumpers-weight.check.ts
  - src/server/checks/robot-bumpers-weight.check.test.ts
  - src/server/routes/check.routes.ts
  - src/server/routes/check.routes.test.ts
  - src/server/checks/engine.test.ts
  - src/server/checks/occurrence-count.check.ts
  - src/server/checks/frame-tag-presence.check.ts
  - src/panel/components/ReportTable.tsx
  - src/panel/components/PlumbingBanner.tsx
  - src/panel/main.tsx
autonomous: true
requirements: [RSLT-01, RSLT-02]

must_haves:
  truths:
    - "The panel shows exactly the 5 real rule checks (MAT-AUDIT, R103, R408, R101, R104) — the two Phase-1 plumbing checks no longer appear (RSLT-01, SC1, D-01)"
    - "Every UNKNOWN verdict row states its own plain-language reason sourced from that check's caveats[0] — never the hardcoded 'N parts missing material' string (RSLT-02, SC2, D-04)"
    - "Both weight checks (R103, R408) emit a self-contained reason as caveats[0] on their UNKNOWN branch, so a missing-material/unresolved-mass weight verdict is honestly explained (RSLT-02, D-04a)"
    - "The UNKNOWN status badge reads 'NOT YET CHECKABLE' (plain language) and stays visually distinct-yellow from the red FAIL badge (RSLT-02, D-04)"
    - "No 'Plumbing proof — verdicts not yet trusted' banner remains on the report view, since the panel is now positioned as trustworthy (D-01, Pitfall 5)"
  artifacts:
    - path: "src/server/checks/robot-weight.check.ts"
      provides: "R103 UNKNOWN branch pushes an explicit reason as caveats[0]"
      contains: "missing material or unresolved mass"
    - path: "src/server/checks/robot-bumpers-weight.check.ts"
      provides: "R408 UNKNOWN branch pushes an explicit reason as caveats[0]"
      contains: "missing material or unresolved mass"
    - path: "src/panel/components/ReportTable.tsx"
      provides: "renderReason() helper + 'NOT YET CHECKABLE' badge relabel"
      contains: "NOT YET CHECKABLE"
    - path: "src/server/routes/check.routes.ts"
      provides: "buildEngine() with the two plumbing checks unregistered (5 checks)"
  key_links:
    - from: "src/panel/components/ReportTable.tsx"
      to: "verdict.caveats"
      via: "renderReason reads caveats[0] for UNKNOWN rows"
      pattern: "caveats\\[0\\]|caveats\\.length"
    - from: "src/server/routes/check.routes.ts"
      to: "engine.runAll"
      via: "buildEngine registers only the 5 real checks"
      pattern: "engine\\.register"
---

<objective>
Make every row on the results panel honest: retire the two Phase-1 proof-of-plumbing checks (resolving the R101/R103 citation collision), fix both weight checks so their UNKNOWN state carries a real reason, and render each not-yet-checkable row with its own plain-language reason under a "NOT YET CHECKABLE" badge — never a silent pass, never the misleading hardcoded "N parts missing material" string.

Purpose: RSLT-01 (each check cites rule/title/limit/measured for the real Phase 1-3 checks) and RSLT-02 (explicit not-yet-checkable state with a plain reason). This is the honest-per-check-state vertical slice: check-function correctness → engine registry → panel rendering, end to end.
Output: 5 real verdicts in /api/check output, honest UNKNOWN reasons for all five checks including the two weight checks, and a trust-positioned panel with no plumbing banner.
</objective>

## Phase Goal

**As a** CAD lead handing the panel to a mentor or inspector, **I want to** see only the real rule checks — each showing pass/fail or an explicit "not yet checkable" reason — **so that** no row ever silently passes or shows a misleading reason for missing data.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-trust-bar-results-panel/04-CONTEXT.md
@.planning/phases/04-trust-bar-results-panel/04-RESEARCH.md
@.planning/phases/04-trust-bar-results-panel/04-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 01)

- `robot-weight.check.ts` UNKNOWN branch: explicit `caveats[0]` reason string (`"${n} part(s) have missing material or unresolved mass — see affected parts"`), existing BUMPER_/BATTERY_ notes appended after it.
- `robot-bumpers-weight.check.ts` UNKNOWN branch: same explicit `caveats[0]` reason, BATTERY_ note appended after.
- `buildEngine()` in `check.routes.ts`: 5 registrations (occurrenceCountCheck + frameTagPresenceCheck removed, imports removed).
- Deleted files: `src/server/checks/occurrence-count.check.ts`, `src/server/checks/frame-tag-presence.check.ts`.
- Rewritten `engine.test.ts`: engine mechanics tested against inline trivial CheckFns instead of the deleted plumbing checks.
- `ReportTable.tsx`: `renderReason()` helper + `"NOT YET CHECKABLE"` badge label (UNKNOWN status key unchanged).
- Deleted file: `src/panel/components/PlumbingBanner.tsx` (removed from `main.tsx`).

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Push an explicit UNKNOWN reason into both weight checks (RSLT-02 backend half, D-04a)</name>
  <files>src/server/checks/robot-weight.check.ts, src/server/checks/robot-weight.check.test.ts, src/server/checks/robot-bumpers-weight.check.ts, src/server/checks/robot-bumpers-weight.check.test.ts</files>
  <read_first>
    - src/server/checks/robot-weight.check.ts (UNKNOWN branch lines 45-56 returns `caveats` = only pre-populated BUMPER_/BATTERY_ presence notes, which can be `[]`)
    - src/server/checks/robot-bumpers-weight.check.ts (near-duplicate; UNKNOWN branch lines 36-47, BATTERY_ note only)
    - src/server/checks/frame-perimeter.check.ts lines 51-89 (the correct idiom: a single self-contained plain-language sentence pushed into caveats where UNKNOWN is decided)
    - src/server/checks/robot-weight.check.test.ts lines 49-102 (existing UNKNOWN-branch tests to extend; note the "emits missing-tag caveats" test at 88 that must still pass)
    - 04-RESEARCH.md "Pitfall 1" table + "Don't Hand-Roll" (why a panel-only fix fails for these two checks)
  </read_first>
  <behavior>
    - RED: extend robot-weight.check.test.ts's UNKNOWN tests ("returns UNKNOWN with affectedParts", line 49; "unresolved (undefined) mass", line 75) to also assert `verdict.caveats[0]` is a non-empty string containing "missing material or unresolved mass". Add the mirror assertion to robot-bumpers-weight.check.test.ts's UNKNOWN test. Run — these FAIL (caveats[0] is currently a tag note or undefined).
    - GREEN: in each check's `if (untrusted.length > 0)` return, set `caveats: [reasonString, ...caveats]` where reasonString = `${untrusted.length} part(s) have missing material or unresolved mass — see affected parts`, so the explicit reason is always caveats[0] and the existing tag-presence notes follow.
    - Preserve: PASS/FAIL branches unchanged, `affectedPartCount`/`affectedParts` unchanged, existing tag-presence caveats still present (now after the reason) so the "emits missing-tag caveats" test still passes.
  </behavior>
  <action>In `robot-weight.check.ts`, change the UNKNOWN-branch `caveats` field (currently bare `caveats`) to prepend an explicit reason string `${untrusted.length} part(s) have missing material or unresolved mass — see affected parts` followed by `...caveats`. Apply the structurally-identical change to `robot-bumpers-weight.check.ts` (its `included` set differs only by the BUMPER_ exclusion; the UNKNOWN return block shape is the same). Do NOT touch material-audit.check.ts (never returns UNKNOWN, per Pitfall 2). Do NOT change the Verdict schema.</action>
  <verify>
    <automated>npx vitest run src/server/checks/robot-weight.check.test.ts src/server/checks/robot-bumpers-weight.check.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `robot-weight.check.ts` UNKNOWN branch returns `caveats[0]` = a non-empty string containing "missing material or unresolved mass".
    - `robot-bumpers-weight.check.ts` UNKNOWN branch returns the same explicit `caveats[0]`.
    - New/extended assertions in both `.test.ts` files assert `caveats[0]` content on the UNKNOWN branch and pass; all pre-existing tests in both files still pass.
    - `npx vitest run src/server/checks/robot-weight.check.test.ts src/server/checks/robot-bumpers-weight.check.test.ts` exits 0.
  </acceptance_criteria>
  <done>Both weight checks emit an explicit UNKNOWN reason as caveats[0]; both weight-check test files pass with the new assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the two plumbing checks from the engine and repair their dependents (RSLT-01, D-01)</name>
  <files>src/server/routes/check.routes.ts, src/server/routes/check.routes.test.ts, src/server/checks/engine.test.ts, src/server/checks/occurrence-count.check.ts, src/server/checks/frame-tag-presence.check.ts</files>
  <read_first>
    - src/server/routes/check.routes.ts lines 8-9 (imports to remove) and lines 35-45 (`buildEngine()` — remove the two `engine.register(...)` lines)
    - src/server/routes/check.routes.test.ts lines 194-244 (the "returns 7 verdicts" test: line 216 asserts length 7, line 238 asserts R101 length 2 — both must change; comment blocks at 232-243 and 381-386 reference the collision)
    - src/server/checks/engine.test.ts (FULL FILE — imports occurrenceCountCheck/frameTagPresenceCheck at lines 3-4 and uses them as the sample CheckFns for engine mechanics at lines 22-65; deleting the check files WILL break this test unless it is rewritten)
    - 04-CONTEXT.md D-01/D-01a/D-01b; 04-RESEARCH.md Open Question 2 (recommends delete)
    - CORRECTION to 04-RESEARCH.md: these checks have NO dedicated `*.check.test.ts` siblings, but `engine.test.ts` depends on them directly — this task must repair engine.test.ts.
  </read_first>
  <action>Unregister both plumbing checks: delete the `engine.register(occurrenceCountCheck)` and `engine.register(frameTagPresenceCheck)` lines in `buildEngine()`, and remove their two `import` lines at the top of `check.routes.ts`. Delete `src/server/checks/occurrence-count.check.ts` and `src/server/checks/frame-tag-presence.check.ts` (per D-01b + RESEARCH recommendation). Rewrite `engine.test.ts` so its `buildEngine()` helper and the "identical Fact[] reference" test register small INLINE trivial `CheckFn`s (e.g. one returning `measuredCount: facts.length`, one returning a fixed PASS/FAIL derived from a config entry) instead of importing the deleted checks — keeping the CheckEngine's generic register/runAll/identical-facts/operator behavior fully tested with zero dependency on retired checks. Remove the two deleted imports from `engine.test.ts`. In `check.routes.test.ts`, change the verdict-count assertion `toHaveLength(7)` → `toHaveLength(5)`, change the R101 filter assertion `toHaveLength(2)` → `toHaveLength(1)`, rename the test title's "7 verdicts" → "5 verdicts", and update the stale collision-comment blocks to note the collision is resolved by D-01. Leave the `measuredContext` `toEqual` assertion (3 fields) unchanged — Plan 02 grows it.</action>
  <verify>
    <automated>npx vitest run src/server/routes/check.routes.test.ts src/server/checks/engine.test.ts; grep -rn "occurrenceCountCheck\|frameTagPresenceCheck" src/server/routes src/server/checks/engine.ts src/server/checks/engine.test.ts || echo "NO_STALE_REFS"</automated>
  </verify>
  <acceptance_criteria>
    - `buildEngine()` in check.routes.ts registers exactly 5 checks (materialAuditCheck, robotWeightCheck, robotBumpersWeightCheck, framePerimeterCheck, startingHeightCheck).
    - `src/server/checks/occurrence-count.check.ts` and `src/server/checks/frame-tag-presence.check.ts` no longer exist.
    - `grep -rn "occurrenceCountCheck\|frameTagPresenceCheck" src/server/routes src/server/checks/engine.ts src/server/checks/engine.test.ts` prints NO_STALE_REFS (no remaining references).
    - `check.routes.test.ts` asserts `res.body.verdicts` has length 5 and exactly 1 verdict with rule "R101".
    - `npx vitest run src/server/routes/check.routes.test.ts src/server/checks/engine.test.ts` exits 0.
  </acceptance_criteria>
  <done>The two plumbing checks are unregistered and deleted, engine.test.ts is rewritten with inline CheckFns, and the route test asserts 5 verdicts / one R101.</done>
</task>

<task type="auto">
  <name>Task 3: Render honest not-yet-checkable rows and remove the plumbing banner (RSLT-01/RSLT-02 panel half, D-04/D-05)</name>
  <files>src/panel/components/ReportTable.tsx, src/panel/main.tsx, src/panel/components/PlumbingBanner.tsx</files>
  <read_first>
    - src/panel/components/ReportTable.tsx lines 7-27 (STATUS_COLORS + the buggy `renderMeasured()` hardcoding "UNKNOWN — N parts missing material") and lines 50-104 (badge render at 68, caveats-row block at 74-104 already maps `verdict.caveats`)
    - src/panel/main.tsx lines 5, 85-101 (`report` branch renders `<PlumbingBanner />` unconditionally; line 91 comment references occurrenceCountCheck)
    - src/panel/components/PlumbingBanner.tsx (file to delete)
    - src/panel/api.ts lines 28-49 (CheckReportVerdict shape — caveats/affectedPartCount available; do NOT change it here)
    - 04-PATTERNS.md "ReportTable.tsx" section (renderReason idiom + badge-relabel guidance) and Pitfall 5
  </read_first>
  <action>In `ReportTable.tsx`: add a small pure `renderReason(verdict)` helper returning `verdict.caveats[0]` when `caveats.length > 0`, else a defensive fallback `not yet checkable${verdict.affectedPartCount ? ` — ${verdict.affectedPartCount} part(s) affected` : ""}`. Replace `renderMeasured()`'s UNKNOWN branch so the Measured cell shows the non-misleading literal `"NOT YET CHECKABLE"` — remove the `"UNKNOWN — ${affectedPartCount} parts missing material"` string entirely; let the existing caveats-row block (lines 94-99, unchanged) surface `caveats[0]` as the reason so the reason is not duplicated. In the badge render (line 68), display `verdict.status === "UNKNOWN" ? "NOT YET CHECKABLE" : verdict.status` while keeping `STATUS_COLORS` keyed by the unchanged `"UNKNOWN"` literal (no Verdict schema change, D-04). Keep the existing inline-style idiom only — no CSS framework, no design tokens (D-05). In `main.tsx`: remove the `<PlumbingBanner />` render (line 87) and its import (line 5), then delete `src/panel/components/PlumbingBanner.tsx`. Keep the `v.geometry`-based HullRender selection as-is (still correct) but update the now-stale comment at line 91 to note the R101/R103 collision is resolved by D-01. Do NOT touch the loading/reconnect/auth-required branches (D-03 clear-then-show-fresh is already correct and must be preserved). Confirm no `dangerouslySetInnerHTML` is introduced (React escapes by default).</action>
  <verify>
    <automated>npm run build && grep -c "NOT YET CHECKABLE" src/panel/components/ReportTable.tsx && (grep -rn "PlumbingBanner" src/panel || echo "NO_BANNER_REFS")</automated>
  </verify>
  <acceptance_criteria>
    - `ReportTable.tsx` renders the label "NOT YET CHECKABLE" (not "UNKNOWN") for UNKNOWN verdicts, and the `renderMeasured()` "N parts missing material" hardcoded string is gone.
    - `ReportTable.tsx` surfaces each UNKNOWN verdict's `caveats[0]` as its reason (via the existing caveats-row block); no `dangerouslySetInnerHTML` present.
    - `grep -rn "PlumbingBanner" src/panel` prints NO_BANNER_REFS and `src/panel/components/PlumbingBanner.tsx` no longer exists.
    - `STATUS_COLORS` still keys `UNKNOWN` to the yellow pair (`#664d03`/`#fff3cd`), distinct from FAIL red.
    - `npm run build` exits 0 (server typecheck + panel build succeed).
  </acceptance_criteria>
  <done>UNKNOWN rows read "NOT YET CHECKABLE" with per-check caveats[0] reasons; the plumbing banner is removed and the panel builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel → /api/check | Untrusted `req.body` (documentId/workspaceId) crosses here; already validated by `CheckRequestSchema` and the target element is re-derived server-side (CONN-02) |
| /api/check → Onshape REST | Server-side calls with the session token; responses are the authoritative source of check facts |
| server JSON → panel render | Verdict fields (rule/title/caveats/affectedParts) rendered into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information disclosure | UNKNOWN reason caveats in weight checks | mitigate | Reason strings are composed from `untrusted.length` (a count) + generic wording only — no part-internal identifiers, tokens, or referenced-document names leak; `affectedParts` already exposes only `name`/`path` (unchanged) |
| T-04-02 | Tampering (XSS) | ReportTable renders caveats[0] / affectedParts names | mitigate | Rendered via React JSX text interpolation only (auto-escaped); task forbids `dangerouslySetInnerHTML`; values originate from server-fetched Onshape data, not raw client HTML |
| T-04-03 | Repudiation / integrity | Retiring checks from buildEngine | accept | Pure list edit; the 5 remaining real checks are unchanged in behavior; regression covered by `check.routes.test.ts` (5 verdicts) and `engine.test.ts` rewrite |
| T-04-SC | Tampering (supply chain) | npm installs | accept | This plan installs ZERO new packages (04-RESEARCH.md: no new dependencies); no legitimacy checkpoint required |
</threat_model>

<verification>
- Full suite green: `npm test` exits 0 after all three tasks.
- `/api/check` output contains exactly 5 verdicts, exactly one citing R101, none citing the retired plumbing checks.
- Both weight checks and all three geometry/height/audit UNKNOWN branches surface a self-contained plain-language reason as `caveats[0]`.
- Panel builds (`npm run build`) and shows "NOT YET CHECKABLE" badges with per-check reasons; no plumbing banner.
</verification>

<success_criteria>
- RSLT-01: the panel lists each of the 5 real checks with rule/title/limit/measured; the two plumbing checks are gone and the R101/R103 collision is resolved.
- RSLT-02: every not-yet-checkable row shows an explicit "NOT YET CHECKABLE" state with a plain-language reason (from caveats[0]) — never a silent pass, never the misleading "parts missing material" string, including for R103/R408.
- `npm test` and `npm run build` both exit 0.
</success_criteria>

<output>
Create `.planning/phases/04-trust-bar-results-panel/04-01-SUMMARY.md` when done.
</output>

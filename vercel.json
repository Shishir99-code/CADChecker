{
  "version": 2,
  "regions": ["iad1"],
  "services": {
    "backend": {
      "root": ".",
      "buildCommand": "tsc -p tsconfig.server.json && vite build",
      "installCommand": "npm install",
      "entrypoint": "server.ts"
    }
  }
}
